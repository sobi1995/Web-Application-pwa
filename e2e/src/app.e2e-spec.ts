import { NgPokedexPage } from './app.po';

describe('Web Application  Pwa App', () => {
  let page: NgPokedexPage;

  beforeEach(() => {
    page = new NgPokedexPage();
  });

  // it('should display heading saying Web Application  Pwa', () => {
  //   page.navigateTo();
  //   expect(page.getHeadingText()).toEqual('Web Application  Pwa');
  // });
});
